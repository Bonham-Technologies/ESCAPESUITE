---
'@escapesuite/artist': patch
---

Split the clip inspector into focused modules — one component per section, the value maths as pure functions, and the store wiring in a hook — with no behaviour change.
