---
'@escapesuite/artist': patch
---

Internal: the editor shell is split into focused modules — the chrome as components, the theme, session, autosave, shortcut, resize and host-integration concerns as hooks — with no behaviour change.
