# ESCSUITE-65 design — a mask (circle / rounded rectangle) and a stroke on every media clip (2026-09-25)

> Status: APPROVED 2026-09-25 with the operator's six decisions folded in (section g, "Decisions"). Follows ESCSUITE-14, whose slice 2 carries the webcam's position and size into ARTIST and left the shape — and, it turns out, the border — to this ticket.

## (a) Recommendation (three sentences)

Add `clip.mask?: { kind: 'circle' | 'rounded'; radius?: number }` as a **static, non-keyframeable** clip field and apply it as one `beginPath → ellipse|roundRect → clip` inside the two existing `save`/`restore` blocks in `apps/artist/src/core/canvasRenderer.ts` — `drawClipToCanvas` (line 325) and `drawImageToCanvasWithModifiers` (line 452), immediately after the rotation block (lines 379-383 / 504-508) and before `drawImage` (lines 389 / 514) — because every pipeline funnels through those two functions and nothing else. Ship it for **media clips only** in v1 (text and shape overlays have no drawn box the mask could mean anything against — `drawTextOverlayToCanvasAnimated`'s geometry comes from `measureText`, canvasRenderer.ts:129), gating the new inspector row exactly as `BlendModeSection` is gated (`ClipEditor.tsx:105`). Then the handoff is genuinely one line in `placeTakeOnTimeline` beside the transform it already maps (`apps/artist/src/store/clipSlice.ts:137-139`), and the `shape`-is-ignored sentences in `apps/artist/src/utils/overlayPlacement.ts:74-75` and `apps/artist/CLAUDE.md:205-206` come out.

Two things the decisions added (section g). **A stroke** is a second static field, `clip.stroke?: { color: string; width: number }`, drawn on the mask's own outline after the image and *outside* the clip region (CRAFT's shape: `overlayGeometry.ts:182-187` restores, re-traces the path and strokes it — so no half of the line is eaten by the mask), and the webcam handoff passes CRAFT's white 3 px border through as one. **A per-clip thumbnail on the timeline**, masked with CSS `clip-path`, because the operator wants the mask visible where the user looks for the clip and ARTIST's timeline draws no picture at all today (`TimelineTrack.tsx:103-140` is a styled `<div>` with a waveform and a type icon; the only `<img>` of `thumbnailUrl` is the media-library card, `VideoUploader.tsx:440`, which is per *source* and so cannot carry a per-clip mask).

## (b) Data model

**Static property, not transform.** Exact field, on `Clip` (`apps/artist/src/store/types.ts:311-338`, after `effects` on line 326):

```ts
export type ClipMaskKind = 'none' | 'circle' | 'rounded';
export interface ClipMask { kind: ClipMaskKind; radius?: number }  // radius: rounded only
mask?: ClipMask;   // absent === { kind: 'none' }
export interface ClipStroke { color: string; width: number }  // width: fraction of the FRAME width (see e); 0 === none
stroke?: ClipStroke;  // absent === no stroke
```

`ClipStroke` deliberately reuses the words `ShapeOverlayData` already uses for its outline (`strokeColor`/`strokeWidth`, types.ts:196-211, default `'#ffffff'` / `0`) so the two vocabularies read alike, but it is its own object on the clip — a shape overlay's stroke belongs to its drawn shape, a media clip's stroke belongs to its mask outline (or its rectangle when `kind` is `'none'`). `width` is a **fraction of the frame width**, for the same reason the inset and the corner radius are fractions (section e): CRAFT's 3 px is 3 px *of a 1280-wide canvas*, and a pixel count would change meaning the moment the project resolution changed.

Why not in `ClipTransform` (types.ts:39-47): every field there is a number fed through `getAnimatedValues` and every one of them is an `AnimatableProperty` (types.ts:95-103). Putting an enum in there would put a non-interpolable value inside the interpolator and force a `DEFAULT_TRANSFORM` change (types.ts:50-58) that every fixture and the migration at `projectMigration.ts:54` reads.

**Keyframing: no, and not "for free".** `radius` is a number, but keyframing it costs a new `AnimatableProperty` member (types.ts:95), a `ClipAnimation.keyframes` key (types.ts:135), a row and a curve in the keyframe panel, and a new field on `AnimatedOverlayValues`/the `animated` object the renderers read. `kind` cannot be interpolated at all, so a keyframed radius with a static kind is a half-feature. Static.

**Migration: none required.** Optional + `undefined === none` means `ensureTimelineHasTracks` (`projectMigration.ts:48-65`) needs no new line — it only defaults fields the renderer dereferences unconditionally. `DB_VERSION` stays 1 (`packages/shared/src/storage/index.ts:9`): the mask lives inside the project blob, not in a new object store. The session snapshot carries `state.project` whole (`apps/artist/src/app/sessionSnapshot.ts:13`), so autosave/restore gets it free. Add one regression test to `projectStore.migration.test.ts` proving a pre-mask project loads with `clip.mask === undefined` and renders unmasked.

## (c) The ONE draw call, and the per-frame cost

There is **one shared renderer and two media entry points into it**, and all three pipelines go through them:

- Preview: `drawFrame.ts:139` (`drawImageToCanvasWithModifiers`) and `drawFrame.ts:152` (`drawClipToCanvas`).
- WebM export: `exportWebM.ts:341` / `exportWebM.ts:347` — the same two functions.
- MP4 export: `exportMP4.ts:450` → `drawMediaWithFrame` (canvasRenderer.ts:428) → the same two.
- Transitions: `drawTransition` (canvasRenderer.ts:592) and `drawTransitionWithFrames` (canvasRenderer.ts:680) both reduce to `drawMediaWithModifiers`/`drawMediaWithFrame` → the same two.
- Headless: `apps/artist/src/headless/renderProject.ts:80-81` calls `exportToWebM`/`exportToMP4` — literally the editor's engine, as `apps/artist/CLAUDE.md:587` says.

So a mask written once in each of those two functions is drawn once in all five paths. Put it **after** the rotation translate/rotate/translate (canvasRenderer.ts:379-383 and 504-508) so it rotates with the clip, and after `x`/`y`/`scaledWidth`/`scaledHeight` are known (385-386 / 510-511), i.e. between line 386 and 389 (and 511 and 514). Both functions already `save()` (339 / 465) and `restore()` (392 / 516), so **no extra save/restore** is needed — and a wipe transition's existing `clip()` (355-360 / 481-486) intersects with the mask, which is the correct composition.

Per masked clip per frame: `beginPath()` + one of `ellipse()` / `roundRect()` + `clip()` = **3 recorded context calls**, no extra `drawImage`, no extra `getAnimatedValues`, no allocation (pass numbers, don't build a `Path2D`). Do not call `closePath()` — `clip()` closes implicitly and it would be a fourth call per clip per frame for nothing (CRAFT's own draw does call it, overlayGeometry.ts:145/170).

Factor the path into a pure `core/clipMask.ts` (`maskPathFor(kind, radius, x, y, w, h)` returning the numbers, plus an `applyClipMask(ctx, …)` that issues the three calls) so the maths is unit-testable and both call sites are one line.

**The stroke's cost, only when present.** The stroke must not be clipped by the mask, so it is drawn after the image with the clip region gone but the rotation kept: the two functions' existing `save()` becomes the *outer* save (transforms), a second `save()` wraps only the mask + `drawImage`, its `restore()` drops the clip region, then `beginPath` + the same path (`ellipse`/`roundRect`/`rect`) + `lineWidth` + `strokeStyle` + `stroke()`, then the outer `restore()`. Per stroked clip per frame: **1 extra save, 1 extra restore, and 6 calls for the outline** (`beginPath`, path, `lineWidth`, `strokeStyle`, `stroke` — `lineWidth`/`strokeStyle` are property sets the canvas double records as calls). A clip with neither mask nor stroke records exactly what it records today — pin that. `saves === restores` stays exact. `strokeStyle` is set from `stroke.color` as given; the line width in canvas pixels is `stroke.width × frame width`, where the frame is the project resolution (the export canvas *is* the project resolution; the preview canvas scales uniformly, so the ratio holds). Put both in `core/clipMask.ts` as `applyClipStroke(ctx, …)` beside `applyClipMask`.

## (d) Editor UI

`ClipEditor/` already has a `ShapeSection.tsx` for shape *overlays* (its `<select>` is at line 26). **Do not reuse that name** — name the new file `MaskSection.tsx` and label it "Mask" (or "Clip Shape"), or the panel will show two "Shape" sections. Copy `BlendModeSection.tsx:17-32` verbatim in shape: a `CollapsibleSection` with `defaultOpen={false}`, one `<select className={styles.select}>` over a new `CLIP_MASK_KINDS` table in `clipEditorOptions.ts` (beside `BLEND_MODES` at line 24 — that file's comment at lines 3-5 makes option order part of the contract), plus a radius `<input type="range">` row in `styles.transformRow` shown only when `kind === 'rounded'` (the same conditional shape as `ShapeSection.tsx:37-53`'s blur row).

The same section carries the stroke, as two more rows under the mask rows: a width slider (0 to `0.02` of the frame width, labelled in pixels at the current project resolution so the user reads "3 px", not "0.0023") and a colour input (`<input type="color">` plus the alpha the CRAFT border needs — store the CSS colour string as given; the colour input writes `#rrggbb`, and a handed-over `rgba(255, 255, 255, 0.8)` shows as white in it and is only replaced when the user picks). A width of 0 means no stroke and the handler writes `stroke: undefined`, never `{ width: 0 }`, so a clip that has never been stroked and one whose stroke was removed are the same object shape.

Wiring: two handlers in `useClipEditorActions.ts`, modelled on `handleBlendModeChange` (lines 205-211), calling the **existing** `updateClip(selectedClip.id, { mask })` / `updateClip(selectedClip.id, { stroke })` (`clipSlice.ts:243`) — it pushes history already (line 265), so no new store action and no new slice member in `ClipSlice`'s `Pick` (clipSlice.ts:16). Render it in `ClipEditor.tsx` beside `BlendModeSection` under the same `{!isAudio && !isOverlay && …}` guard (line 105). The selector contract in `ClipEditor.rerender.test.tsx` (its header, lines 1-29) and `useClipEditorActions.ts:16-24` is the hard rule: **no new store subscription, and never `currentTime`** — read the mask off `selectedClip`, which the panel already has. The rerender test's mount/tick counts must come out unchanged; if the new section moves them, the section is subscribing to something it shouldn't.

## (e) The handoff mapping

`TakeClipPart.overlayPlacement` already arrives on the webcam part (`app/takeImport.ts:154-156`) and `placeTakeOnTimeline` already converts it to a transform (`clipSlice.ts:137-139`). The mapping is one property beside it:

```ts
mask: part.overlayPlacement ? maskForPlacement(part.overlayPlacement, overlayFrame ?? resolution) : undefined,
```

with a new `maskForPlacement` in `utils/overlayPlacement.ts` returning `{ kind: 'circle' }` for `'circle'` and `{ kind: 'rounded', radius: … }` for `'rectangle'`.

The CRAFT numbers, from `apps/craft/src/core/overlayGeometry.ts`: circle is an **inscribed circle**, `radius = Math.min(webcamWidth, webcamHeight) / 2` centred on the box (lines 139-142), *plus* a centre-crop of the source (155-181); rectangle is `ctx.roundRect(x, y, w, h, 8)` — a flat **8 px** (line 167) in pixels of a canvas capped at `COMPOSITOR_MAX_WIDTH = 1280` (line 27).

So express the radius as a **fraction of the clip's shorter side**, exactly as `OVERLAY_MARGIN_FRACTION = 20 / 1280` did for the inset (`overlayPlacement.ts:23`), because the pixel count is meaningless at another resolution (the same argument as that constant's doc, lines 17-22) and ARTIST has a resolution-change dialog that would otherwise silently change the corner rounding. Add `export const OVERLAY_CORNER_RADIUS_FRACTION = 8 / 1280;` and derive: radius-in-frame-px `= frame.width * 8/1280`, so the stored fraction `= (frame.width * 8/1280) / Math.min(overlayWidth, overlayHeight)` — both already computed at `overlayPlacement.ts:88` and `:97`.

The **border** maps the same way: `strokeForPlacement()` returns `{ color: 'rgba(255, 255, 255, 0.8)', width: OVERLAY_STROKE_WIDTH_FRACTION }` for every webcam part, with `export const OVERLAY_STROKE_WIDTH_FRACTION = 3 / 1280;` and `export const OVERLAY_STROKE_COLOR = 'rgba(255, 255, 255, 0.8)';` — the two literals CRAFT hard-codes inline at `overlayGeometry.ts:185-186` and `:204-205`, named once here and, in the same change, named in CRAFT too so the two cannot drift (a pure move on the CRAFT side: the composited preview and the composite MP4 draw the same pixels before and after, pinned by the existing compositor and converter tests being byte-unchanged).

Docs sentence to replace, `overlayPlacement.ts:74-75`: "`placement.shape` is read and **ignored** — a mask on every clip is ESCSUITE-65, and when it exists the circle maps onto it here." → it now maps, naming the two constants. Same for `apps/artist/CLAUDE.md:205-206`.

## (f) Task list

1. **`ClipMask` + `ClipStroke` types, defaults, migration hygiene** — `store/types.ts:311-338`; red-first: `projectStore.migration.test.ts` (pre-mask project → `mask === undefined` and `stroke === undefined`, renders unmasked and unstroked), `app/sessionSnapshot.test.ts` (both survive a snapshot round trip). Ceilings: none. **0.5 PR-day.**
2. **`core/clipMask.ts` + the canvas double learns `roundRect`** — `maskPathFor`, `applyClipMask`, `applyClipStroke`; new `core/clipMask.test.ts` (circle = inscribed radius `min(w,h)/2` at the box centre; rounded radius = fraction × `min(w,h)`, clamped to `min(w,h)/2`; `radius <= 0` and `kind:'none'` issue no mask; a stroke with `kind:'none'` traces `rect`; `width <= 0` issues nothing; the rounded path is built from `arcTo` when `roundRect` is missing — see (h)); `src/test/doubles/canvas.ts` has no `roundRect` (its method table is lines 150-162, its type at 59-64) so the rounded path cannot be recorded until it does. Ceilings: none. **0.75 PR-day.**
3. **Apply mask and stroke in the two media draws** — `core/canvasRenderer.ts` at 386-389 and 511-514; red-first in `core/canvasRenderer.clips.test.ts`: `clip()` recorded *after* `rotate()` and *before* `drawImage()`; the ellipse args are the drawn box's centre and half-extents; an unmasked, unstroked clip records exactly today's calls; `save` count == `restore` count in every combination (mask only, stroke only, both); a stroked clip's `stroke()` comes after `drawImage()` and after the inner `restore()`; a masked clip inside a wipe records two `clip()`s. Ceilings: unchanged (the perf scene has no masks — `test/fixtures/perfScene.ts:108` builds none). **1 PR-day.**
4. **Masked and stroked per-frame ceilings** — a masked+stroked variant of the scene in `test/fixtures/perfScene.ts`, new cases in `components/Preview/drawFrame.perf.test.ts` (beside the ceilings at lines 160-174) and `core/exportMP4.perf.test.ts` (beside 184-201): the plain ceilings must not move; the masked ceiling is measured +3 calls per masked clip, the stroked +8 (1 save, 1 restore, 6 outline calls), `saves === restores` exact, `drawImagesPerFrame` and `animationLookupsPerFrame` unchanged. **0.5 PR-day.**
5. **`MaskSection` + `CLIP_MASK_KINDS` + the two handlers** — new `ClipEditor/MaskSection.tsx` and `MaskSection.test.tsx` (mask kind select, radius slider for `rounded`, stroke width slider labelled in px at the project resolution, stroke colour input), `clipEditorOptions.ts` (+`clipEditorOptions.test.ts`), `useClipEditorActions.ts` (+ its test), `ClipEditor.tsx:105`; red-first: choosing "Circle" calls `updateClip` with `{ mask: { kind: 'circle' } }` and pushes one history entry; the radius slider only exists for `rounded`; width 0 writes `stroke: undefined`; the section is absent for audio and for overlay clips. Ceilings: `ClipEditor.rerender.test.tsx` counts unchanged. **1.25 PR-days.**
6. **Preview parity test** — `components/Preview/PreviewPlayer.rendering.test.tsx`: a masked and stroked clip in the store makes the preview issue the same calls in the same order as the export does for the same clip (the shared-renderer claim, `apps/artist/CLAUDE.md:587`, made a test). **0.5 PR-day.**
7. **The handoff mapping, both apps** — `utils/overlayPlacement.ts` (new `maskForPlacement`, `strokeForPlacement`, `OVERLAY_CORNER_RADIUS_FRACTION`, `OVERLAY_STROKE_WIDTH_FRACTION`, `OVERLAY_STROKE_COLOR`), `store/clipSlice.ts:137-139`; CRAFT `core/overlayGeometry.ts` names its two inline literals (pure move: compositor and converter tests byte-unchanged); red-first in `utils/overlayPlacement.test.ts` (`'circle'` → `{kind:'circle'}`; `'rectangle'` → rounded whose radius reproduces CRAFT's 8 px at 1280 and scales with the frame; every webcam part → the white 3/1280 stroke) and the `placeTakeOnTimeline` take test (the webcam clip carries mask and stroke, the primary carries neither). **0.75 PR-day.**
8. **The timeline clip thumbnail, masked** — `components/Timeline/TimelineTrack.tsx:103-140`: a media clip (video or image source with a `thumbnailUrl`) renders one `<img aria-hidden alt="" draggable={false}>` of its source's thumbnail at the head of the clip, fixed height (the clip row's inner height), 16:9, `pointer-events: none`, with `style={{ clipPath }}` from a pure `utils/maskClipPath.ts` (`maskClipPathFor(mask, thumbHeightPx)` → `circle(<h/2>px at <h/2>px 50%)` for circle (the circle hugs the thumbnail's **left** edge rather than being centred in the 16:9 box — amended by the slice-2 whole-branch review, because a clip narrower than the thumb is `overflow: hidden` and a centred circle would show it nothing; only the centre moved, the radius is still `maskPathFor`'s), `inset(0 round <fraction × h>px)` for rounded, `undefined` for none — the thumb's shorter side is its height, so the fraction resolves against it exactly as the canvas path does). The stroke is **not** drawn on the thumbnail (v1; say so). Red-first: a video clip renders the img with its source's URL; an audio clip and a clip whose source has no thumbnail render none; the three mask kinds give the three `clipPath` values; the thumb is inside `.clipContent` so the existing drag/trim/marquee hit geometry (`timelineGeometry.ts`) is untouched. Ceilings: `components/Timeline/timelineGestures.perf.test.ts` counts unchanged (an `<img>` is not a listener, a rect read or a render); `App.rerender.test.tsx` Timeline render counts unchanged (no new subscription — the source lookup is the one `AudioWaveform` already uses). **0.75 PR-day.**
9. **Headless + e2e parity** — `services/headless-artist/test/ffprobe.ts` gains a `frameCornerRGB` sibling (`crop=8:8:0:0,scale=1:1`; today's `frameMeanRGB` at line 71 averages the whole frame and would blur the signal) and a `frameEdgeRGB` for the stroke; a masked+stroked case in `services/headless-artist/src/verify.chromium.test.ts` built by **patching the loaded fixture in-test**, the way the two-clip case is built at lines 97-112 and `apps/e2e/tests/headless/render-bundle.spec.ts:86` patches resolution — leave `apps/e2e/fixtures/headless/project.json` untouched so no existing golden moves. Assertion: corner ≈ black, centre ≈ red, a point on the circle's edge ≈ white. Plus one ESCAPEARTIST e2e (`take-import.spec.ts` gains a case, or a sibling): a handed-over webcam part shows "Circle" in the inspector's Mask section and a stroke width of 3 px at 1280×720. **0.75 PR-day.**
10. **Docs + changesets** — `apps/artist/CLAUDE.md:205-206` (and the draw-path note near 587; the timeline thumbnail note), `utils/overlayPlacement.ts:74-75`, `apps/craft/CLAUDE.md` (the named border constants), root `CLAUDE.md`'s take-handoff bullet (the webcam clip now arrives masked and stroked), `ESCAPE-SUITE-DOCUMENTATION.md`, an artist changeset (minor: two new clip properties, a timeline thumbnail) and a craft changeset (patch: constants only, no behaviour). **0.25 PR-day.**

Total ≈ 7 PR-days, in two slices: **slice 1** tasks 1–7 (the mask, the stroke, the inspector, the handoff — the ticket as filed), **slice 2** tasks 8–10 (the thumbnail, parity, docs). Slice 1 ships on its own; slice 2 is what decisions 5 and 6 added beyond the original ask.

## (g) Decisions (operator, 2026-09-25)

1. **Circle is the inscribed circle**, `min(w,h)/2` centred on the drawn box — what the user saw in CRAFT (`overlayGeometry.ts:139-142`). No fitted ellipse; a separate `'ellipse'` kind can be added later if box-filling is ever wanted.
2. **Radius is a fraction of the clip's shorter side** (0–0.5), never pixels; `OVERLAY_CORNER_RADIUS_FRACTION = 8/1280` reproduces CRAFT's 8 px at the compositor cap.
3. **Media clips only** in v1. Text and shape overlays take neither mask nor stroke; the inspector section is gated exactly as `BlendModeSection` is.
4. **Not keyframeable.** `kind` never; `radius` and the stroke "later, if asked" — neither joins `AnimatableProperty`.
5. **The mask shows in the thumbnail.** "So the user sees what they expect to see." ARTIST's timeline draws no clip picture today, so this adds a per-clip masked thumbnail to media clips on the timeline (task 8, CSS `clip-path`, no canvas). The preview's selection box and hit test stay rectangular (`hitTest.ts`, `selectionOverlay.ts`), stated in the docs so it is not read as a bug. The media-library card is per source and stays unmasked.
6. **Strokes become a clip layer option of their own**, `clip.stroke`, applied where appropriate — on the mask's outline, or the rectangle when there is no mask — and the CRAFT handoff passes the webcam's white 3 px border through as `{ color: 'rgba(255, 255, 255, 0.8)', width: 3/1280 }`.

## (h) Risks

- **`ctx.roundRect` support.** Safari gained it in 16.4 and the e2e suite runs WebKit (`pnpm test:e2e:browsers`); CRAFT already calls it unconditionally (`overlayGeometry.ts:171`) but only in a Chromium-favoured recording path. Build the rounded path from `arcTo` in `core/clipMask.ts`, or feature-detect and fall back — a throw here kills the whole preview frame, not just the mask.
- **Name collision** with the existing `ShapeSection`/`ShapeOverlayData` vocabulary (`ClipEditor/ShapeSection.tsx`, `store/types.ts:200-226`). Choosing the word "shape" for the new field would make every future grep ambiguous; `mask` keeps them apart.
- **Two draw functions, not one.** `drawClipToCanvas` and `drawImageToCanvasWithModifiers` are near-duplicates (canvasRenderer.ts:325 vs 452); adding the mask to one and not the other gives videos a mask and images none. The shared helper plus a test per function is the guard.
- **The blur-shape scratch path** re-captures the canvas at the identity transform and hands it back (canvasRenderer.ts:244-247). A masked clip *under* a blur shape is fine, but a masked clip drawn *after* one is worth one explicit test.
- **Perf-scene drift.** Adding a mask to an existing perf-scene clip would move the plain ceilings and break the "same scene as the browser benchmark" contract (`perfScene.ts:1-16`, `apps/e2e/utils/perf.ts`). Add a *variant*, never edit the scene.
- **The timeline thumbnail is DOM, not canvas.** One `<img>` per media clip is cheap, but the timeline's per-pointer-move layout count is a published benchmark number (`timeline-interaction`); keep the img fixed-size, `pointer-events: none`, `draggable={false}`, inside `.clipContent`, and re-run that benchmark once before and after task 8 so a layout regression is seen rather than guessed. `clip-path` is universally supported; `inset(… round …)` needs no prefix in the three engines the e2e suite runs.
- **Fixture blast radius.** `apps/e2e/fixtures/headless/project.json` backs at least five specs plus the service's manifest tests; patch in-test rather than editing it.
